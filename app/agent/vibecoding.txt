分析这个前端代码，完成以下工作：
1. 为前端代码生成对应的后端代码，确保前后端联通，实现一个前后端一体的app
2. 实现数据存储，采用supabase数据库

为这个应用，实现一个pc端的后台管理功能，要求如下：
1.ui风格和这个app保持一致
2.单独实现一个，不要和现有代码混杂，不要修改现有代码
3.功能包括：
	录入宠物信息，图片使用cloudflare R2存储，当用户上传图片，把图片上传到 cloudflare R2，并返回图片url。
	查看所有宠物信息。
	审批用户的领养申请，审批通过后在app中发消息通知用户，并更新领养状态，而且要在app端把被领养的宠物下线，但是不要在管理后台下线。